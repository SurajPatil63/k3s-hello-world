apiVersion: v1
kind: Service
metadata:
  name: hello-world-nginx
  namespace: hello-world
  labels:
    app: hello-world-nginx
spec:
  type: NodePort
  selector:
    app: hello-world-nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
